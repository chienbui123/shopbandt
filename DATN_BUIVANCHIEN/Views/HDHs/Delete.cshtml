@model DATN_BUIVANCHIEN.Models.HDH

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutClient.cshtml";
}
<a href="@Url.Action("Index", "Admin")" style="font:30px">Về trang trủ</a>
<h2>Delete</h2>

<h3>Bạn đã sẵn sàng xóa loại hệ điều hành ?</h3>
<div>
    <h4>HDH</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.TenHDH)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TenHDH)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Xóa" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
@if(ViewBag.Error != null)
            {
                <br />
                <div class="alert alert-danger" role="alert">@ViewBag.Error</div>
            }